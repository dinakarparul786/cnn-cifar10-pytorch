# cnn-cifar10-pytorch
This repository contains my implementation of a Convolutional Neural Network (CNN) using the CIFAR-10 dataset, one of the most popular benchmark datasets in computer vision. The project demonstrates the end-to-end workflow of building, training, and evaluating a CNN model with PyTorch and TorchVision.
